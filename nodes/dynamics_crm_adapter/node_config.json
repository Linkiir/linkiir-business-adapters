{
  "guid": "",
  "workflow_guid": "",
  "node_type": "source",
  "node_type_id": "SOURCE_CUSTOM",
  "catalog_type_id": "LKBZ_DYNAMICS_CRM_ADAPTER",
  "interval": true,
  "node_name": "Dynamics CRM Adapter",
  "description": "Polls a Microsoft Dynamics 365 CRM instance with a FetchXML query on each interval and pushes each matching record downstream as JSON.",
  "config": [
    {
      "label": "Interval",
      "value": 60000,
      "field_type": "number",
      "description": "How often, in milliseconds, the runtime invokes the polling script."
    },
    {
      "label": "URL",
      "value": "",
      "field_type": "string",
      "description": "Base URL of the Dynamics 365 instance, e.g. https://org.crm.dynamics.com/. The Web API path (api/data/v9.2/) is appended to it, so a trailing slash is expected (one is added if omitted)."
    },
    {
      "label": "Username",
      "value": "",
      "field_type": "string",
      "description": "Azure AD username for the Resource Owner Password Credentials grant."
    },
    {
      "label": "Password",
      "value": "",
      "field_type": "password",
      "description": "Password for the Azure AD user account."
    },
    {
      "label": "FetchXML Query",
      "value": "<fetch distinct=\"false\" mapping=\"logical\" output-format=\"xml-platform\" version=\"1.0\">\n   <entity name=\"ccx_master_queue\">\n      <attribute name=\"ccx_name\"/>\n      <attribute name=\"createdon\"/>\n      <attribute name=\"ccx_recordtype\"/>\n      <attribute name=\"ccx_processeddate\"/>\n      <attribute name=\"ccx_processed\"/>\n      <attribute name=\"ccx_interfacename\"/>\n      <attribute name=\"createdby\"/>\n      <attribute name=\"ccx_master_queueid\"/>\n      <order descending=\"true\" attribute=\"createdon\"/>\n      <filter type=\"and\">\n         <condition attribute=\"statecode\" value=\"0\" operator=\"eq\"/>\n         <condition attribute=\"createdon\" value=\"{{curr_date}}\" operator=\"on\"/>\n      </filter>\n   </entity>\n</fetch>",
      "field_type": "string",
      "description": "The FetchXML query to execute on each poll. Use {{curr_date}} as a placeholder for today's date (YYYY-MM-DD). The entity name in the query determines which entity set is queried."
    },
    {
      "label": "Live Mode",
      "value": true,
      "field_type": "bool",
      "description": "When off, requests are simulated and no data leaves the runtime. Authentication is always performed live so credential problems surface immediately."
    },
    {
      "label": "Verify TLS",
      "value": true,
      "field_type": "bool",
      "description": "Whether to verify the Dynamics server's TLS certificate. Leave on outside of local testing against a proxy."
    }
  ],
  "library_deps": {
    "dynamics_crm": "1.0.0"
  },
  "config_version": 1,
  "version": "1.0.0"
}
