{
  "name": "salesforce",
  "description": "Salesforce REST API client. Handles OAuth 2.0 client_credentials authentication and provides query, modify, modifyBatch, delete and custom request helpers. Copy the salesforce/ folder into a node and add it to package.path.",
  "version": "1.0.0"
}
